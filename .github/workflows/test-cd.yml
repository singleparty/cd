name: Manually triggered workflow
on:
  push:
    branches:
      - master
jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Hello ${{ github.actor }}!"
          echo "- in ${{ github.ref }}!"